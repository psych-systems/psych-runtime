// The conversation is rendered by the `(chat)` layout, which stays mounted
// across `/chat` -> `/chat/{id}`. This route exists so that address resolves.
export default function ChatPage() {
  return null;
}
